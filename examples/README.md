# Examples

This directory is reserved for public, non-sensitive examples.

Do not place private PCB images, enterprise inspection images, training labels, or runtime result images here.

Suggested local-only files:

```text
samples/pcb.jpg
runtime/output/result.jpg
```

These files are ignored by Git.
