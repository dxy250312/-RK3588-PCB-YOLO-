# Models

Place local model binaries in this directory when running the project.

Expected examples:

```text
models/best-rk3588.rknn
```

Model binaries are intentionally ignored by Git and should not be committed.
